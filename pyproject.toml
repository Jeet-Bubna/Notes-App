[project]
name = "todo-react-django"
version = "0.1.0"
description = "An app which is made by react and django."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "django>=6.0.3",
]

[dependency-groups]
dev = [
    "django-debug-toolbar>=6.2.0",
]
